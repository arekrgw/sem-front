import { Box, Typography } from "@mui/material";

interface StatisticsProps {}

const Statistics = (props: StatisticsProps) => {
  return (
    <Box>
      <Typography fontSize="24px" component="h2">
        Statystyki
      </Typography>
      TBD
    </Box>
  );
};

export default Statistics;
